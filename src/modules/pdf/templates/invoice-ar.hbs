<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="UTF-8">
    <title>فاتورة ضريبية</title>
    <style>
        body { font-family: 'Arial', sans-serif; margin: 0; padding: 20px; direction: rtl; }
        .header { display: flex; justify-content: space-between; margin-bottom: 30px; }
        .company-info { flex: 1; text-align: right; }
        .logo { max-width: 150px; max-height: 80px; }
        .invoice-title { font-size: 24px; font-weight: bold; color: #333; text-align: left; }
        .invoice-details { margin: 20px 0; }
        .customer-info { margin: 20px 0; padding: 15px; background: #f9f9f9; }
        .items-table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        .items-table th, .items-table td { border: 1px solid #ddd; padding: 8px; text-align: right; }
        .items-table th { background-color: #f2f2f2; }
        .totals { margin-top: 20px; text-align: left; }
        .total-row { margin: 5px 0; }
        .final-total { font-weight: bold; font-size: 18px; }
        .qr-code { margin-top: 20px; text-align: center; }
        .footer { margin-top: 30px; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="header">
        <div class="company-info">
            {{#if company.logoUrl}}
            <img src="{{company.logoUrl}}" alt="شعار الشركة" class="logo">
            {{/if}}
            <h2>{{company.nameAr}}</h2>
            <p>{{company.addressAr}}</p>
            <p>{{company.cityAr}}، {{company.countryCode}}</p>
            <p>البريد الإلكتروني: {{company.email}}</p>
            <p>الهاتف: {{company.phone}}</p>
            {{#if company.trn}}
            <p><strong>الرقم الضريبي: {{company.trn}}</strong></p>
            {{/if}}
        </div>
        <div class="invoice-title">
            فاتورة ضريبية
        </div>
    </div>

    <div class="invoice-details">
        <p><strong>رقم الفاتورة:</strong> {{invoice.invoiceNumber}}</p>
        <p><strong>تاريخ الإصدار:</strong> {{invoice.issueDate}}</p>
        {{#if invoice.dueDate}}
        <p><strong>تاريخ الاستحقاق:</strong> {{invoice.dueDate}}</p>
        {{/if}}
        <p><strong>العملة:</strong> {{invoice.currencyCode}}</p>
    </div>

    <div class="customer-info">
        <h3>فاتورة إلى:</h3>
        <p><strong>{{customer.nameAr}}</strong></p>
        <p>{{customer.addressAr}}</p>
        <p>{{customer.cityAr}}، {{customer.countryCode}}</p>
        {{#if customer.email}}
        <p>البريد الإلكتروني: {{customer.email}}</p>
        {{/if}}
        {{#if customer.trn}}
        <p><strong>الرقم الضريبي: {{customer.trn}}</strong></p>
        {{/if}}
    </div>

    <table class="items-table">
        <thead>
            <tr>
                <th>الوصف</th>
                <th>الكمية</th>
                <th>سعر الوحدة</th>
                <th>معدل الضريبة</th>
                <th>المجموع</th>
                <th>مبلغ الضريبة</th>
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <td>{{this.descriptionAr}}</td>
                <td>{{this.quantity}}</td>
                <td>{{this.unitPrice}} {{../invoice.currencyCode}}</td>
                <td>{{this.vatRate}}%</td>
                <td>{{this.lineTotal}} {{../invoice.currencyCode}}</td>
                <td>{{this.lineVatAmount}} {{../invoice.currencyCode}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <div class="totals">
        <div class="total-row">المجموع الفرعي: {{invoice.subtotal}} {{invoice.currencyCode}}</div>
        {{#if invoice.discountAmount}}
        <div class="total-row">الخصم: -{{invoice.discountAmount}} {{invoice.currencyCode}}</div>
        {{/if}}
        <div class="total-row">ضريبة القيمة المضافة ({{invoice.vatRate}}%): {{invoice.vatAmount}} {{invoice.currencyCode}}</div>
        <div class="total-row final-total">المجموع الكلي: {{invoice.totalAmount}} {{invoice.currencyCode}}</div>
    </div>

    {{#if invoice.notesAr}}
    <div style="margin-top: 20px;">
        <strong>ملاحظات:</strong>
        <p>{{invoice.notesAr}}</p>
    </div>
    {{/if}}

    {{#if qrCode}}
    <div class="qr-code">
        <img src="{{qrCode}}" alt="رمز الاستجابة السريعة" style="width: 100px; height: 100px;">
        <p>امسح للتحقق</p>
    </div>
    {{/if}}

    <div class="footer">
        <p>هذه فاتورة مُنشأة بواسطة الحاسوب ولا تتطلب توقيعاً.</p>
        <p>تم الإنشاء في {{invoice.createdAt}}</p>
    </div>
</body>
</html>